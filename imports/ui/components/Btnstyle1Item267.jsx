import React from 'react';

const Btnstyle1Item267 = ({ divText }) => {
  return (
    <a href={'#'} className={'btn-style1'}>
      <div>{divText || 'Add'}</div>
    </a>
  );
};

export default Btnstyle1Item267;
