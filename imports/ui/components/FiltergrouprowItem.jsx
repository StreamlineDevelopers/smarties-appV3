import React from 'react';

const FiltergrouprowItem = ({}) => {
  return (
    <div className={'filter-group_row'}>
      <div className={'tag-multi-select'}></div>
    </div>
  );
};

export default FiltergrouprowItem;
