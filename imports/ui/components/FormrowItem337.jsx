import React from 'react';
import FormcontrolItem335 from './FormcontrolItem335';

const FormrowItem337 = ({ label, name, dataName, id }) => {
  return (
    <div className={'form-row'}>
      <FormcontrolItem335
        label={'Inbound Phone Number'}
        name={'inbound-form-number'}
        dataName={'inbound form number'}
        id={'inbound-form-number'}
      />
    </div>
  );
};

export default FormrowItem337;
