import React from 'react';

const FileinforowItem272 = ({ label, divText }) => {
  return (
    <div className={'file-info-row'}>
      <div className={'file-info-label'}>{label || 'Knowledge Base ID'}</div>
      <div className={'file-info-data'}>{divText || '97dcd1234567'}</div>
    </div>
  );
};

export default FileinforowItem272;
