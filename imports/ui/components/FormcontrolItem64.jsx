import React from 'react';

const FormcontrolItem64 = ({ label }) => {
  return (
    <div className={'form-control'}>
      <div className={'form-label'}>{label || 'Upload Documents'}</div>
    </div>
  );
};

export default FormcontrolItem64;
