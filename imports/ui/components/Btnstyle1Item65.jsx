import React from 'react';

const Btnstyle1Item65 = ({ handleCancel }) => {
  return (
    <a href={'#'} className={'btn-style1 outline'} onClick={handleCancel}>
      <div>Cancel</div>
    </a>
  );
};

export default Btnstyle1Item65;
