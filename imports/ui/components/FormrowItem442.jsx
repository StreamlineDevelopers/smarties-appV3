import React from 'react';
import FormcontrolItem437 from './FormcontrolItem437';

const FormrowItem442 = ({ label, id, name, dataName }) => {
  return (
    <div className="form-row">
      <FormcontrolItem437
        label={label || 'Tool Description'}
        id={id || 'tool-description-2'}
        name={name || 'tool-description-2'}
        dataName={dataName || 'Tool Description 2'}
      />
    </div>
  );
};

export default FormrowItem442;
