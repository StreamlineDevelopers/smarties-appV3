import React from 'react';
import FormcontrolItem407 from './FormcontrolItem407';

const FormrowItem410 = ({ label, name, dataName, type, id }) => {
  return (
    <div className={'form-row'}>
      <FormcontrolItem407
        label={'Domain'}
        name={'domain-name'}
        dataName={'domain name'}
        type={'url'}
        id={'domain-name-2'}
      />
    </div>
  );
};

export default FormrowItem410;
