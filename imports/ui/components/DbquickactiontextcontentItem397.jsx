import React from 'react';

const DbquickactiontextcontentItem397 = ({ title, divText }) => {
  return (
    <div className="db-quickaction-textcontent">
      <div className="insights-title">
        {title || 'Your last tweet performed 80% above avg!'}
      </div>
      <div className="insights-subtext">
        {divText || 'Try similar topics to maintain engagement.'}
      </div>
    </div>
  );
};

export default DbquickactiontextcontentItem397;
