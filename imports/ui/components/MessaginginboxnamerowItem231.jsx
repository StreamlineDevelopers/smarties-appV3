import React from 'react';

const MessaginginboxnamerowItem231 = ({ divText }) => {
  return (
    <div className={'messaging-inbox-name-row'}>
      <div>{divText || 'Alex Johnson'}</div>
    </div>
  );
};

export default MessaginginboxnamerowItem231;
