import React from 'react';
import FormcontrolItem370 from './FormcontrolItem370';

const FormrowItem371 = ({ label, name, dataName, id }) => {
  return (
    <div className={'form-row'}>
      <FormcontrolItem370
        label={'New Password'}
        name={'new-password'}
        dataName={'new password'}
        id={'new-password'}
      />
    </div>
  );
};

export default FormrowItem371;
