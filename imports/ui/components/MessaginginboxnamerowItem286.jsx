import React from 'react';

const MessaginginboxnamerowItem286 = ({ divText }) => {
  return (
    <div className={'messaging-inbox-name-row'}>
      <div>{divText || 'John Smith'}</div>
    </div>
  );
};

export default MessaginginboxnamerowItem286;
