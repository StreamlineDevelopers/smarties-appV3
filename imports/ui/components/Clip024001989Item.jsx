import React from 'react';

const Clip024001989Item = ({ id, transform }) => {
  return (
    <clippath id={id || 'clip0_2400_1989'}>
      <rect
        width="14"
        height="14"
        fill="white"
        transform={transform || 'translate(0.375)'}
      ></rect>
    </clippath>
  );
};

export default Clip024001989Item;
