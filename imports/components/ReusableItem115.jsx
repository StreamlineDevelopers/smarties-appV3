import React from 'react';
import Clip024001989Item from './Clip024001989Item';

const ReusableItem115 = ({ id, transform }) => {
  return (
    <defs>
      <Clip024001989Item
        id={id || 'clip0_2400_1989'}
        transform={transform || 'translate(0.375)'}
      />
    </defs>
  );
};

export default ReusableItem115;
