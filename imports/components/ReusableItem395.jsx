import React from 'react';

const ReusableItem395 = ({ label, divText }) => {
  return (
    <div>
      <div className="headline-item-label">
        {label || '#MarketingAutomation'}
      </div>
      <div className="headline-body-text">
        {divText || 'How AI is transforming digital marketing'}
      </div>
    </div>
  );
};

export default ReusableItem395;
