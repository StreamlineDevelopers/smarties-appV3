import React from 'react';

const InboxitemItem457 = ({ divText }) => {
  return (
    <a href="#" className="inbox-item w-inline-block">
      <div>{divText || 'Configuration 01'}</div>
    </a>
  );
};

export default InboxitemItem457;
