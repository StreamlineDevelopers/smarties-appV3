import React from 'react';

const MessaginginboxnamerowItem329 = ({ divText }) => {
  return (
    <div className="messaging-inbox-name-row">
      <div>{divText || 'John Smith'}</div>
    </div>
  );
};

export default MessaginginboxnamerowItem329;
